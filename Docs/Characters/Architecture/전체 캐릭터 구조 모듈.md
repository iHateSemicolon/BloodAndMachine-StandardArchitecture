```mermaid
graph TB
    %% 1. 메인 타이틀과 전체 구조를 하나로 묶음
    subgraph Main ["# 모든 캐릭터들 같은 이 구조로 짜주세요! (//모듈 스크립트 필요)"]
        direction TB

        %% Gui 섹션
        subgraph Section1 ["## Gui"]
            subgraph ScreenGui ["ScreenGui"]
                subgraph mainFrame ["mainFrame"]
                    TextButton["TextButton"]
                end
            end
        end

        %% char(rig) 섹션
        subgraph Section2 ["## char(rig)"]
            subgraph Rig ["Rig"]
                subgraph HumanoidRootPart ["HumanoidRootPart"]
                    DamagePart["DamagePart(Part)"]
                end
            end
        end

        %% Skills 섹션
        subgraph Section3 ["## Skills"]
            SkillsText["캐릭터마다 달라지겠지만 각 스킬마다 전용 히트박스 필요"]
        end

        %% 예시 구조 세션 연결
        subgraph Section4 ["### 예시 구조 (#### D계급)"]
            direction TB
            
            %% 예시 Gui
            subgraph D_Gui ["##### Gui"]
                subgraph D1 ["D-levelSkillGui(screenGui)"]
                    subgraph D2 ["D-levelESkillFrame(mainFrame)"]
                        D3["D-levelEskill(TextButton)"]
                    end
                end
            end

            %% 예시 char
            subgraph D_Char ["##### char"]
                subgraph DC1 ["D-level(rig)"]
                    subgraph DC2 ["HumanoidRootPart"]
                        DC3["D-levelDamageBox(Part)"]
                    end
                end
            end

            %% 예시 E-Skill
            subgraph D_Skill ["##### E-Skill ( D계급은 현재 유니온 설치 능력만 있음 )"]
                subgraph DS1 ["union"]
                    DS2["unionHitbox(part)"]
                end
            end

        end
    end
