# 모든 캐릭터들 같은 이 구조로 짜주세요!
//모듈 스크립트 필요

```markdown
```mermaid
graph TD
    %% 첫 번째 섹션: Gui
    subgraph Gui ["[ Gui ]"]
        ScreenGui --> mainFrame["mainFrame"]
        mainFrame --> TextButton["TextButton"]
    end

    %% 두 번째 섹션: char(rig)
    subgraph char["[ char(rig) ]"]
        Rig --> HumanoidRootPart["HumanoidRootPart"]
        HumanoidRootPart --> DamagePart["DamagePart(Part)"]
    end

    %% 세 번째 섹션: Skills
    subgraph Skills ["[ Skills ]"]
        direction TB
        SkillText["캐릭터마다 달라지겠지만<br>각 스킬마다 전용 히트박스 필요"]
    end


graph TD
    %% 예시 Gui
    subgraph Gui_D ["[ Gui ]"]
        D1["D-levelSkillGui(screenGui)"] --> D2["D-levelESkillFrame(mainFrame)"]
        D2 --> D3["D-levelEskill(TextButton)"]
    end

    %% 예시 char
    subgraph char_D ["[ char ]"]
        DC1["D-level(rig)"] --> DC2["HumanoidRootPart"]
        DC2 --> DC3["D-levelDamageBox(Part)"]
    end

    %% 예시 E-Skill
    subgraph E-Skill ["[ E-Skill ( D계급은 현재 유니온 설치 능력만 있음 ) ]"]
        DS1["union"] --> DS2["unionHitbox(part)"]
    end
